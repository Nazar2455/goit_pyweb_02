[tool.poetry]
name = "personaly-assistent"
version = "0.1.0"
description = "Personal assistent project"
authors = ["Nazar245"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
